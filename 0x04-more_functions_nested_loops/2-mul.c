#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: product of the two given numbers
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
